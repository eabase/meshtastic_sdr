The -data file here is actually an IQ file. And the -meta file is a SigMF JSON definition. 

If you want to simulate RX flow in Gnuradio, you can inject using a file source and give the -data file on repeat. 